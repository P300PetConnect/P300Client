export interface IUser {
        name: string;
        surname: string;
        dob: Date;
        profilePicUrl: string;
        mobileNumber: string;
        emailAddress: string;
        petOwnerId: number;
    }

